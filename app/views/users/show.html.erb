<div>
  <% if @user.microposts.any? %>
    <h3><%= @user.sc_user_name %> (<%= @user.microposts.count %>)</h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
  <% end %>
</div>